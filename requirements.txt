# FastAPI + server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# HTTP + async
httpx==0.27.2
aiohttp==3.10.11

# Harvesters (synchronous HTTP + HTML parsing)
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Supabase + Postgres
supabase==2.9.0
postgrest==0.17.2

# Data
pydantic==2.9.2
email-validator==2.2.0
python-dotenv==1.0.1

# PDF rendering
reportlab==4.2.5
xhtml2pdf==0.2.16
# xhtml2pdf converts our letter HTML to print-ready PDFs for Stannp's
# mail-merge endpoint. Pure Python (built on reportlab) — no Cairo/Pango
# system libs needed, no Nixpacks lib-path drama. CSS support is more
# limited than WeasyPrint's but sufficient for our letter renderer
# (inline styles only, no flexbox/grid). Used by
# backend/services/letter_pdf_renderer.py.
# Switched from weasyprint==62.3 on 2026-06-01 after Nixpacks couldn't
# locate libpango/libglib in the Python ctypes search path.

# Anthropic (narrative generation only, NOT decision-making)
anthropic==0.42.0

# Stripe (carried from v1 — not product logic)
stripe==11.3.0

# Utility
python-dateutil==2.9.0
pytz==2024.2
pypdf>=3.0.0
